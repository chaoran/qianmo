<div class="comments <%= "full" unless local_assigns[:full_mode].nil? %>">
  <% for comment in post.comments %>
    <%= render 'comments/show', :comment => comment %>
  <% end %>
  <%= render 'comments/new', :post => post %>
</div>

