<p><%= t :hello %><%= @resource.name %>,</p>

<p><%= t :reset_password_email_1 %></p>

<p>
  <%= link_to "#{edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)}", 
      edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %>
</p>

<p><%= t :reset_password_email_2 %></p>
