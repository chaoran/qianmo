<%= my_link_to "#{@user.is_male?() ? t('follow_him') : t('follow_her')}", 
               user_follow_path(@user), 
               :class => "btn btn-success", :id => "follow_button", 
               :icon => "icon-plus", :remote => true, :method => :post %>