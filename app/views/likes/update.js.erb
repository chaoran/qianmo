$(".post_<%= params[:id] %>.post-actions .like-label").replaceWith("<%= j like_label(@post) %>")
<% if @like && !@like.notification.nil? %>
  <%= render :partial => "notifications/update", :locals => {:user => @like.notification.user} %>
<% end %>